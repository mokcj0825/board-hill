# Local Development Environment Variables
# 请将此文件重命名为 .env 并放在 hill/game-master/ 目录下

# 本地 Docker PostgreSQL 连接
DATABASE_URL="postgresql://gamemaster:dev_password@localhost:5432/gamemaster_db?schema=public"

# 服务端口
PORT=3001

# 生产环境使用 Cloud SQL 时，取消注释并配置以下项：
# DATABASE_URL="postgresql://USER:PASSWORD@/DB_NAME?host=/cloudsql/PROJECT:REGION:INSTANCE"
# CLOUD_SQL_CONNECTION_NAME=project:region:instance

