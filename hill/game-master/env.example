# Example env (no secrets)
# =============================================================================
# DATABASE_URL Configuration for Cloud SQL
# =============================================================================

# Option 1 (RECOMMENDED): TCP Connection via 127.0.0.1
# ------------------------------------------------------
# Local dev: Run Cloud SQL Proxy first:
#   ./cloud-sql-proxy me-in-react:asia-southeast2:hill --port 5432
# Cloud Run: Add connection in "Connections" tab: me-in-react:asia-southeast2:hill
DATABASE_URL="postgresql://uname:password@127.0.0.1:5432/postgres?schema=public"

# Option 2: Unix Socket (Cloud Run ONLY, won't work locally)
# -----------------------------------------------------------
# DATABASE_URL="postgresql://uname:password@localhost/postgres?host=/cloudsql/me-in-react:asia-southeast2:hill&schema=public"

# Option 3: Direct Public IP (NOT recommended for production)
# ------------------------------------------------------------
# DATABASE_URL="postgresql://uname:password@YOUR_PUBLIC_IP:5432/postgres?schema=public"

PORT=3001
